import NewVerificationForm from '@/components/auth/new-verification-form';
import { FC } from 'react';

interface NewVerificationProps {
}

const NewVerification: FC<NewVerificationProps> = ({ }) => {
  return (
    <NewVerificationForm />
  )
}

export default NewVerification;